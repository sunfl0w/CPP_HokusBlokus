#pragma once

namespace HokusBlokus::Blokus {
    /**
     * @brief A simple enum giving different MoveTypes
     * 
     */
    enum class MoveType {
        SetMove,
        SkipMove,
        PassMove,
    };
}